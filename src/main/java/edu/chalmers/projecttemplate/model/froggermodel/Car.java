package edu.chalmers.projecttemplate.model.froggermodel;

public class Car extends Obstacle {
    public Car(int x, int y, int width, int height, int velocity) {
        super(x, y, width, height, velocity);
    }
}
